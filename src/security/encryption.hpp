#ifndef ENCRYPTION_HPP
#define ENCRYPTION_HPP

#include <iostream>

/**
 * @brief Simulates encryption algorithms.
 */
class Encryption {
public:
    /**
     * @brief Encrypts the data.
     * 
     * This method simulates the process of encrypting data.
     */
    void encryptData();

    /**
     * @brief Decrypts the data.
     * 
     * This method simulates the process of decrypting data.
     */
    void decryptData();
};

#endif // ENCRYPTION_HPP
