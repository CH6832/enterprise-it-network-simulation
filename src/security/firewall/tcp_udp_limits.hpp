#ifndef TCP_UDP_LIMITS_HPP
#define TCP_UDP_LIMITS_HPP

#include <iostream>

/**
 * @brief Limits TCP/UDP access for the firewall.
 */
class TcpUdpLimits {
public:
    /**
     * @brief Limits access to TCP and UDP protocols.
     * 
     * This method simulates the enforcement of access control
     * policies for TCP and UDP traffic, enhancing network
     * security by restricting unwanted or malicious traffic.
     */
    void limitAccess();
};

#endif // TCP_UDP_LIMITS_HPP
